#ifndef PAGES_H
#define PAGES_H

#include <render/document.h>

namespace Pages {
    void statics(Document*);
    void home(Document*);
    void user(Document*);
    void JSONUser(Document*);
    void track(Document*);
    void JSONTrack(Document*);
}

#endif // PAGES_H
