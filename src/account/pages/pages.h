#ifndef ACCOUNT_PAGES_H
#define ACCOUNT_PAGES_H

#include <render/document.h>

namespace Pages {
    void account(Document*);
    void users(Document*);
    void login(Document*);
    void JSONLogin(Document*);
    void registration(Document*);
    void passwordReset(Document*);
    void userActions(Document*);
}

#endif // ACCOUNT_PAGES_H
